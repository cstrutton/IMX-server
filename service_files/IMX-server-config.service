[Unit]
# Human readable name of the unit
Description=IMX PLC Gateway config web service

[Service]
# Command to execute when the service is started
ExecStart=/home/debian/venv/bin/python /home/debian/flask-config/main.py

[Install]
WantedBy=default.target